# Итоги проделанной работы

## 1. Что было сделано

- Проведена предобработка данных и базовый EDA: распределение целевой переменной, наличие выбросов, корреляции.
- Реализованы базовые модели регрессии (LinearRegression, Ridge, Lasso).
- Проведен подбор гиперпараметров с помощью GridSearchCV.
- Реализованы методы отбора признаков (L0 и L1-подходы).
- Создана кастомная метрика, учитывающая, что недопрогноз хуже перепрогноза.

---

## 2. С какими результатами

- Простая линейная регрессия показала базовое качество (ориентир).
- Ridge улучшила устойчивость к мультиколлинеарности, но почти не изменила ошибки.
- Lasso обнулила часть признаков и дала прирост качества за счёт feature selection.
- Модель с кастомной метрикой сместила прогноз “вверх”, уменьшив долю критичных недопрогнозов.

---

## 3. Что дало наибольший буст качества

### 🔥 1. Масштабирование признаков
- StandardScaler перед линейными моделями → стабилизировало веса.
- Без него регуляризация работала хуже и подбор гиперпараметров терял смысл.

### 🔥 2. Правильная постановка GridSearch
- k-fold (CV) вместо train-test подбора → уменьшение переобучения.
- Метрика — не только MSE/MAE, но и кастомная с бизнес-наказанием.

### 🔥 3. L1 (Lasso)
- Убрала дублирующие и шумовые признаки → улучшила устойчивость модели.
- Сократила размерность без необходимости ручного отбора.

---

## 4. Что сделать не вышло и почему

### ❗️ Полноценная L0-регуляризация
- sklearn **не поддерживает L0 напрямую** (NP-hard задача).
- При brute-force переборе количество комбинаций растёт экспоненциально.
- Корректное решение требует методов релаксации (HardConcrete, Gumbel) или жадных алгоритмов (RFECV).

### ❗️ Применение dummy-весов для отбора
- Малый коэффициент ≠ неважный признак.
- Dummy отражают разницу относительно базовой категории, поэтому
  **удаление по величине веса приводит к потере информации**.
- Корректные подходы: L1, групповые тесты (ANOVA), RFECV.

---

## 5. Выводы

- Регуляризация и кросс-валидация критичны для качества линейных моделей.
- Lasso — оптимальный компромисс между интерпретируемостью и качеством.
- Метрики должны учитывать бизнес-логику: симметричные ошибки вводят ложные выводы.
- Метод L0 пригоден только с ограничениями или вспомогательными алгоритмами — brute-force не масштабируется.

